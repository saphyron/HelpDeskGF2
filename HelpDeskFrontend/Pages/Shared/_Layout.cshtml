<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - HelpDesk</title>
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>

@{
    var isLoggedIn = Context.Session.GetInt32("UserId") != null;
    var username = Context.Session.GetString("Username");
    var role = Context.Session.GetString("Role");
}

<header>
    <!-- DESKTOP NAV -->
    <nav class="desktop-nav">
        <a asp-page="/Home/Homepage">Home</a>
        <a asp-page="/Tickets/TicketList">Tickets</a>
        <a asp-page="/Threads/Forum">Forum</a>

        @if (isLoggedIn)
        {
            <span>Hello, @username</span>

            @if (role == "admin")
            {
                <a asp-page="/Admin/AdminTickets">Admin</a>
            }

            <form method="post" action="/user/logout" style="display:inline;">
                <button type="submit" class="logout-btn">Log out</button>
            </form>
        }
        else
        {
            <a asp-page="/Home">Login</a>
        }
    </nav>

    <!-- MOBILE BURGER -->
    <div class="mobile-nav">
        <partial name="BurgerMenu" />
    </div>
</header>

<main>
    @RenderBody()
</main>

<footer>
    <p>HelpDesk © 2026</p>
</footer>

</body>
</html>